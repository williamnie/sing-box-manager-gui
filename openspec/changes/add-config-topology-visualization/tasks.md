# Tasks: 工作拓扑可视化

## 1. 数据建模与转换

- [x] 1.1 定义前端拓扑数据模型（节点、边、告警、分层）
- [x] 1.2 实现 `config preview -> topology graph` 转换器
- [x] 1.3 实现默认出口（final）与规则链路映射
- [x] 1.4 补充异常检测（缺失引用、孤立节点、空层）

## 2. 拓扑 UI 与交互

- [x] 2.1 在设置页增加“拓扑”展示区域/页签
- [x] 2.2 渲染三层主视图：Inbounds → Route/Policy → Outbounds
- [x] 2.3 支持节点点击查看详情（来源字段、关联关系）
- [x] 2.4 支持链路高亮与基础缩放/滚动
- [x] 2.5 显示静态拓扑说明与告警面板

## 3. 质量与验证

- [x] 3.1 为转换器补充单元测试（典型配置、异常配置）
- [ ] 3.2 为关键渲染逻辑补充最小回归测试
- [x] 3.3 执行 `npm run lint` 与 `npm run build` 验证（`test`/`lint`/`build` 已通过）

## 4. 文档

- [x] 4.1 更新 README 或开发文档，说明拓扑图语义与限制
- [x] 4.2 在 OpenSpec 任务中记录性能与兼容性验证结果
